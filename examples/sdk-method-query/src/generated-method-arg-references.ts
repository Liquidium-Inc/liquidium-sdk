// Generated by scripts/generate-method-arg-references.mjs. Do not edit by hand.

export const METHOD_ARG_REFERENCES: Record<string, string> = {
  "accounts.prepareCreateProfile":
    "client.accounts.prepareCreateProfile(options: PrepareCreateProfileOptions): Promise<CreateAccountAction>\n\ninterface PrepareCreateProfileOptions {\n  /** Wallet address that will own the new profile. */\n  account: string;\n}",
  "accounts.createProfile":
    'client.accounts.createProfile(params: CreateProfileParams): Promise<string>\n\ninterface CreateProfileParams {\n  /** Wallet address that will own the new profile. */\n  account: string;\n  /** Chain used to sign the profile-creation message. */\n  chain: SigningChain;\n  /** Wallet adapter used to sign the profile-creation message. */\n  walletAdapter: WalletAdapter;\n}\n\ntype SigningChain = "BTC" | "ETH";',
  "accounts.getProfileId":
    "client.accounts.getProfileId(walletAddress: string): Promise<string | null>\n\nNo request object type.",
  "accounts.getWalletNonce":
    "client.accounts.getWalletNonce(walletAddress: string): Promise<bigint>\n\nNo request object type.",
  "accounts.listLinkedWallets":
    "client.accounts.listLinkedWallets(profileId: string): Promise<Wallet[]>\n\nNo request object type.",
  "market.listPools":
    "client.market.listPools(): Promise<Pool[]>\n\nNo request object type.",
  "market.getAssetPrices":
    "client.market.getAssetPrices(): Promise<AssetPrices>\n\nNo request object type.",
  "market.findPool":
    'client.market.findPool(query: AssetIdentifier): Promise<Pool>\n\ntype FindPoolQuery = AssetIdentifier;\n\ntype AssetIdentifier =\n  | { chain: "BTC"; asset: "BTC" }\n  | { chain: "ETH"; asset: "USDC" }\n  | { chain: "ETH"; asset: "USDT" }\n  | { chain: "ICP"; asset: "BTC" }\n  | { chain: "ICP"; asset: "ICP" }\n  | { chain: "ICP"; asset: "USDC" }\n  | { chain: "ICP"; asset: "USDT" };',
  "market.getPoolRate":
    "client.market.getPoolRate(poolId: string): Promise<PoolRate>\n\nNo request object type.",
  "market.getReserveData":
    'client.market.getReserveData(query: AssetIdentifier): Promise<Pool>\n\ntype FindPoolQuery = AssetIdentifier;\n\ntype AssetIdentifier =\n  | { chain: "BTC"; asset: "BTC" }\n  | { chain: "ETH"; asset: "USDC" }\n  | { chain: "ETH"; asset: "USDT" }\n  | { chain: "ICP"; asset: "BTC" }\n  | { chain: "ICP"; asset: "ICP" }\n  | { chain: "ICP"; asset: "USDC" }\n  | { chain: "ICP"; asset: "USDT" };',
  "positions.getPosition":
    "client.positions.getPosition(profileId: string, poolId: string): Promise<Position | null>\n\nNo request object type.",
  "positions.listPositions":
    "client.positions.listPositions(profileId: string): Promise<Position[]>\n\nNo request object type.",
  "positions.getHealthFactor":
    "client.positions.getHealthFactor(profileId: string): Promise<HealthFactor>\n\nNo request object type.",
  "positions.getUserStats":
    "client.positions.getUserStats(profileId: string): Promise<UserStats>\n\nNo request object type.",
  "positions.getUserPositionSummary":
    "client.positions.getUserPositionSummary(profileId: string): Promise<UserPositionSummary>\n\nNo request object type.",
  "positions.getUserReserves":
    "client.positions.getUserReserves(profileId: string): Promise<UserReserve[]>\n\nNo request object type.",
  "positions.getMaxRepayAmount":
    "client.positions.getMaxRepayAmount(profileId: string, poolId: string, bufferBps?: bigint): Promise<MaxRepayAmount>\n\nNo request object type.",
  "positions.getFullWithdrawAmount":
    "client.positions.getFullWithdrawAmount(profileId: string, poolId: string): Promise<FullWithdrawAmount>\n\nNo request object type.",
  "activities.list":
    'client.activities.list(request: ListActivitiesRequest): Promise<Activity[]>\n\ntype ListActivitiesRequest =\n  | ListActivitiesByProfileRequest\n  | ListActivitiesByShortRefRequest;\n\ninterface ListActivitiesByProfileRequest\n  extends BaseListActivitiesRequest {\n  /** Profile principal text to list activities for. */\n  profileId: string;\n}\n\ninterface ListActivitiesByShortRefRequest\n  extends BaseListActivitiesRequest {\n  /** Instant-loan short reference to list activities for. */\n  shortRef: string;\n}\n\ninterface BaseListActivitiesRequest {\n  /** Optional lifecycle filter; defaults to `active`. */\n  filter?: ActivityFilter;\n}\n\ntype ActivityFilter = "active" | "completed" | "all";',
  "instantLoans.create":
    'client.instantLoans.create(request: CreateInstantLoanRequest): Promise<InstantLoan>\n\ninterface CreateInstantLoanRequest {\n  /** Collateral leg: pool, asset, and amount the user deposits. */\n  collateral: CreateInstantLoanCollateral;\n  /** Borrow leg: pool, asset, amount, delivery chain, and destination. */\n  borrow: CreateInstantLoanBorrow;\n  /** Refund leg: chain and destination for returned collateral. */\n  refund: CreateInstantLoanRefund;\n  /**\n   * Maximum allowed loan-to-value ratio in basis points.\n   *\n   * `6_000n` means 60%. Use `client.quote.calculateLtv(...)` to calculate the\n   * implied LTV for the selected amounts and pass the policy value your app is\n   * willing to accept. Creation is rejected if the requested borrow would exceed\n   * this limit.\n   */\n  ltvMaxBps: bigint;\n  /**\n   * Seconds allowed for the user to send collateral after loan creation.\n   *\n   * If the collateral deposit is not detected before this window expires, the\n   * instant-loan flow can time out. Internally this is sent to the canister as\n   * `ltv_timer_s`.\n   */\n  depositWindowSeconds: bigint;\n}\n\ninterface CreateInstantLoanCollateral {\n  /**\n   * Principal text of the pool that receives the user\'s collateral deposit.\n   *\n   * This should be the `id` of the collateral `Pool` selected from\n   * `client.market.listPools()`. The pool asset must match `asset`.\n   */\n  poolId: string;\n  /**\n   * Asset the user will deposit as collateral.\n   *\n   * Must match the asset for `poolId`; for example, use `"BTC"` with a BTC\n   * collateral pool.\n   */\n  asset: InstantLoanAsset;\n  /**\n   * Intended credited collateral amount, in base units.\n   *\n   * This is used to validate LTV and initialize the loan record before\n   * deposit/inflow fees are deducted. For BTC, pass satoshis. For token assets,\n   * convert the UI amount using the selected pool\'s `decimals` value. After\n   * creation, use one of `loan.initialDeposit.targets` as the fee-inclusive\n   * transfer quote and destination.\n   */\n  amount: bigint;\n}\n\ntype CreateInstantLoanBorrow = AssetIdentifier & {\n  /**\n   * Principal text of the pool that funds the borrow.\n   *\n   * This should be the `id` of the borrow `Pool` selected from\n   * `client.market.listPools()`. The pool asset must match `asset`.\n   */\n  poolId: string;\n  /**\n   * Amount to borrow, in the borrow asset\'s base units.\n   *\n   * For USDC/USDT, convert the UI amount using the selected borrow pool\'s\n   * `decimals` value before passing it here.\n   */\n  amount: bigint;\n  /**\n   * Destination that receives the borrowed asset after the loan starts.\n   *\n   * Pass either a string shorthand or a typed destination. For BTC/ETH chain\n   * outflows this is usually the user\'s chain address. Chain-key assets on ICP\n   * require an `IcPrincipal`; native ICP also accepts `IcpAccountIdentifier`\n   * and `IcrcAccount` destinations.\n   */\n  destination: InstantLoanDestination;\n};\n\ninterface CreateInstantLoanRefund {\n  /** Delivery chain used for collateral refunds and withdrawals. Use ICP for ck-ledger delivery. */\n  chain: Chain;\n  /**\n   * Destination that receives collateral refunds or withdrawals.\n   *\n   * Pass either a string shorthand or a typed destination. For BTC/ETH chain\n   * outflows this is usually the user\'s chain address. Chain-key assets on ICP\n   * require an `IcPrincipal`; native ICP also accepts `IcpAccountIdentifier`\n   * and `IcrcAccount` destinations.\n   */\n  destination: InstantLoanDestination;\n}\n\ntype InstantLoanAsset = "BTC" | "ICP" | "USDC" | "USDT";\n\ntype AssetIdentifier =\n  | { chain: "BTC"; asset: "BTC" }\n  | { chain: "ETH"; asset: "USDC" }\n  | { chain: "ETH"; asset: "USDT" }\n  | { chain: "ICP"; asset: "BTC" }\n  | { chain: "ICP"; asset: "ICP" }\n  | { chain: "ICP"; asset: "USDC" }\n  | { chain: "ICP"; asset: "USDT" };\n\ntype InstantLoanDestination = LiquidiumAccountInput;\n\ntype Chain = "BTC" | "ETH" | "ICP";\n\ntype LiquidiumAccountInput = string | LiquidiumAccountReference;\n\ninterface LiquidiumAccountReference {\n  /** Address, principal, ICRC account, or ICP account identifier. */\n  address: string;\n  /** Account type hint. Use string shorthand when the SDK should auto-detect. */\n  type: LiquidiumAccountType;\n}\n\ntype LiquidiumAccountType = "ChainAddress" | "IcPrincipal" | "IcpAccountIdentifier" | "IcrcAccount";',
  "instantLoans.getByRef":
    "client.instantLoans.get(request: InstantLoanGetRequest): Promise<InstantLoan>\n\ntype InstantLoanGetRequest =\n  | InstantLoanGetByIdRequest\n  | InstantLoanGetByRefRequest;\n\ninterface InstantLoanGetByIdRequest {\n  /** Canister-assigned loan id. */\n  loanId: bigint;\n}\n\ninterface InstantLoanGetByRefRequest {\n  /** Short user-facing reference derived from `loanId`. */\n  ref: string;\n}",
  "instantLoans.getByLoanId":
    "client.instantLoans.get(request: InstantLoanGetRequest): Promise<InstantLoan>\n\ntype InstantLoanGetRequest =\n  | InstantLoanGetByIdRequest\n  | InstantLoanGetByRefRequest;\n\ninterface InstantLoanGetByIdRequest {\n  /** Canister-assigned loan id. */\n  loanId: bigint;\n}\n\ninterface InstantLoanGetByRefRequest {\n  /** Short user-facing reference derived from `loanId`. */\n  ref: string;\n}",
  "instantLoans.find":
    "client.instantLoans.find(query: string): Promise<InstantLoanFindResult[]>\n\nNo request object type.",
  "activities.getStatus":
    "client.activities.getStatus(request: GetActivityStatusRequest): Promise<GetActivityStatusResponse>\n\ntype GetActivityStatusRequest =\n  | GetActivityStatusByProfileRequest\n  | GetActivityStatusByShortRefRequest;\n\ninterface GetActivityStatusByProfileRequest\n  extends BaseGetActivityStatusRequest {\n  /** Profile principal text that owns the activity. */\n  profileId: string;\n}\n\ninterface GetActivityStatusByShortRefRequest\n  extends BaseGetActivityStatusRequest {\n  /** Instant-loan short reference that owns the activity. */\n  shortRef: string;\n}\n\ninterface BaseGetActivityStatusRequest {\n  /** Activity or receipt id to look up. */\n  id: string;\n}",
  "history.getUserTransactionHistory":
    'client.history.getUserTransactionHistory(user: string, filters?: UserTransactionHistoryFilters): Promise<PaginatedResponse<UserTransactionHistoryEntry>>\n\ninterface UserTransactionHistoryFilters {\n  /** Pagination cursor from a previous response. */\n  cursor?: string;\n  /** Maximum number of entries to return. */\n  limit?: number;\n  /** Market filter accepted by the SDK API. */\n  market?: string;\n  /** Pool principal text filter. */\n  poolId?: string;\n  /** Transaction operation filters. */\n  operations?: UserTransactionHistoryOperation[];\n  /** Lifecycle state filters. */\n  states?: UserTransactionHistoryState[];\n  /** Inclusive start timestamp filter accepted by the SDK API. */\n  from?: string;\n  /** Inclusive end timestamp filter accepted by the SDK API. */\n  to?: string;\n}\n\ntype UserTransactionHistoryOperation = "deposit" | "borrow" | "repayment" | "withdrawal" | "liquidation";\n\ntype UserTransactionHistoryState = "completed" | "action_required" | "confirming" | "processing" | "failed";\n\ntype LiquidiumOperation = "deposit" | "borrow" | "repayment" | "withdrawal" | "liquidation";\n\ntype LiquidiumState = "active" | "completed" | "action_required" | "confirming" | "processing" | "failed" | "expired";',
  "history.getLiquidationHistory":
    "client.history.getLiquidationHistory(user: string, filters?: UserLiquidationHistoryFilters): Promise<PaginatedResponse<UserLiquidationHistoryEntry>>\n\ninterface UserLiquidationHistoryFilters {\n  /** Pagination cursor from a previous response. */\n  cursor?: string;\n  /** Maximum number of entries to return. */\n  limit?: number;\n  /** Market filter accepted by the SDK API. */\n  market?: string;\n  /** Pool principal text filter. */\n  poolId?: string;\n  /** Inclusive start timestamp filter accepted by the SDK API. */\n  from?: string;\n  /** Inclusive end timestamp filter accepted by the SDK API. */\n  to?: string;\n}",
  "quote.getQuote":
    "client.quote.getQuote(request: QuoteRequest, pools: Pool[], prices: AssetPrices): QuoteResult\n\ninterface QuoteRequest {\n  /** Requested borrow amount in borrow asset base units. */\n  borrowAmount: bigint;\n  /** Pool principal text for the borrow side. */\n  borrowPoolId: string;\n  /** Pool principal text for the collateral side. */\n  collateralPoolId: string;\n  /** Target loan-to-value ratio in basis points. */\n  targetLtvBps: bigint;\n}",
  "lending.prepareWithdraw":
    'client.lending.prepareWithdraw(request: CreateWithdrawRequest): Promise<WithdrawAction>\n\ninterface CreateWithdrawRequest {\n  /** Liquidium profile principal text. */\n  profileId: string;\n  /** Pool principal text to withdraw from. */\n  poolId: string;\n  /**\n   * Amount to withdraw in the pool asset\'s base units. BTC withdrawals require\n   * at least 5,000 sats. USDC and USDT withdrawals require at least 1 token.\n   */\n  amount: bigint;\n  /** Chain where withdrawn funds should arrive. */\n  chain: Chain;\n  /** Destination that receives the withdrawn asset. */\n  receiver: LiquidiumAccountInput;\n  /** Wallet address that signs the withdraw authorization. */\n  signerWalletAddress: string;\n}\n\ntype Chain = "BTC" | "ETH" | "ICP";\n\ntype LiquidiumAccountInput = string | LiquidiumAccountReference;\n\ninterface LiquidiumAccountReference {\n  /** Address, principal, ICRC account, or ICP account identifier. */\n  address: string;\n  /** Account type hint. Use string shorthand when the SDK should auto-detect. */\n  type: LiquidiumAccountType;\n}\n\ntype LiquidiumAccountType = "ChainAddress" | "IcPrincipal" | "IcpAccountIdentifier" | "IcrcAccount";',
  "lending.withdraw":
    'client.lending.withdraw(params: CreateWithdrawRequest & WalletExecutionParams): Promise<WithdrawOutflowDetails>\n\ninterface CreateWithdrawRequest {\n  /** Liquidium profile principal text. */\n  profileId: string;\n  /** Pool principal text to withdraw from. */\n  poolId: string;\n  /**\n   * Amount to withdraw in the pool asset\'s base units. BTC withdrawals require\n   * at least 5,000 sats. USDC and USDT withdrawals require at least 1 token.\n   */\n  amount: bigint;\n  /** Chain where withdrawn funds should arrive. */\n  chain: Chain;\n  /** Destination that receives the withdrawn asset. */\n  receiver: LiquidiumAccountInput;\n  /** Wallet address that signs the withdraw authorization. */\n  signerWalletAddress: string;\n}\n\ninterface WalletExecutionParams {\n  /** Chain used by the signing wallet. */\n  signerChain: SigningChain;\n  /** Wallet adapter used to execute the prepared action. */\n  signerWalletAdapter: WalletAdapter;\n}\n\ntype Chain = "BTC" | "ETH" | "ICP";\n\ntype LiquidiumAccountInput = string | LiquidiumAccountReference;\n\ntype SigningChain = "BTC" | "ETH";\n\ninterface LiquidiumAccountReference {\n  /** Address, principal, ICRC account, or ICP account identifier. */\n  address: string;\n  /** Account type hint. Use string shorthand when the SDK should auto-detect. */\n  type: LiquidiumAccountType;\n}\n\ntype LiquidiumAccountType = "ChainAddress" | "IcPrincipal" | "IcpAccountIdentifier" | "IcrcAccount";',
  "lending.prepareBorrow":
    'client.lending.prepareBorrow(request: CreateBorrowRequest): Promise<BorrowAction>\n\ninterface CreateBorrowRequest {\n  /** Liquidium profile principal text. */\n  profileId: string;\n  /** Borrow pool principal text. */\n  poolId: string;\n  /** Amount to borrow in the borrow asset\'s base units. */\n  amount: bigint;\n  /** Chain where borrowed funds should arrive. */\n  chain: Chain;\n  /** Destination that receives the borrowed asset. */\n  receiver: LiquidiumAccountInput;\n  /** Wallet address that signs the borrow authorization. */\n  signerWalletAddress: string;\n}\n\ntype Chain = "BTC" | "ETH" | "ICP";\n\ntype LiquidiumAccountInput = string | LiquidiumAccountReference;\n\ninterface LiquidiumAccountReference {\n  /** Address, principal, ICRC account, or ICP account identifier. */\n  address: string;\n  /** Account type hint. Use string shorthand when the SDK should auto-detect. */\n  type: LiquidiumAccountType;\n}\n\ntype LiquidiumAccountType = "ChainAddress" | "IcPrincipal" | "IcpAccountIdentifier" | "IcrcAccount";',
  "lending.borrow":
    'client.lending.borrow(params: CreateBorrowRequest & WalletExecutionParams): Promise<BorrowOutflowDetails>\n\ninterface CreateBorrowRequest {\n  /** Liquidium profile principal text. */\n  profileId: string;\n  /** Borrow pool principal text. */\n  poolId: string;\n  /** Amount to borrow in the borrow asset\'s base units. */\n  amount: bigint;\n  /** Chain where borrowed funds should arrive. */\n  chain: Chain;\n  /** Destination that receives the borrowed asset. */\n  receiver: LiquidiumAccountInput;\n  /** Wallet address that signs the borrow authorization. */\n  signerWalletAddress: string;\n}\n\ninterface WalletExecutionParams {\n  /** Chain used by the signing wallet. */\n  signerChain: SigningChain;\n  /** Wallet adapter used to execute the prepared action. */\n  signerWalletAdapter: WalletAdapter;\n}\n\ntype Chain = "BTC" | "ETH" | "ICP";\n\ntype LiquidiumAccountInput = string | LiquidiumAccountReference;\n\ntype SigningChain = "BTC" | "ETH";\n\ninterface LiquidiumAccountReference {\n  /** Address, principal, ICRC account, or ICP account identifier. */\n  address: string;\n  /** Account type hint. Use string shorthand when the SDK should auto-detect. */\n  type: LiquidiumAccountType;\n}\n\ntype LiquidiumAccountType = "ChainAddress" | "IcPrincipal" | "IcpAccountIdentifier" | "IcrcAccount";',
  "lending.supply":
    'client.lending.supply(request: SupplyFlowRequest): Promise<SupplyFlow>\n\ntype SupplyFlowRequest =\n  | TransferSupplyFlowRequest\n  | ContractInteractionSupplyFlowRequest;\n\ntype TransferSupplyFlowRequest =\n  | ManualTransferSupplyFlowRequest\n  | WalletTransferSupplyFlowRequest;\n\ninterface ContractInteractionSupplyFlowRequest\n  extends BaseSupplyFlowRequest {\n  /** Contract-interaction mechanism discriminator. */\n  mechanism: "contractInteraction";\n  /** Contract interaction is only supported for ETH stablecoin pools. */\n  chain: "ETH";\n  /** ETH wallet adapter used to approve and deposit ERC-20 assets. */\n  walletAdapter: Pick<WalletAdapter, "sendEthTransaction">;\n  /** Sender EVM wallet address. */\n  account: string;\n  /** Deposit or repayment amount in token base units. */\n  amount: bigint;\n}\n\ninterface ManualTransferSupplyFlowRequest extends BaseSupplyFlowRequest {\n  /** Transfer supply uses the default mechanism and does not accept this field. */\n  mechanism?: never;\n  /** Manual supply does not broadcast through a wallet adapter. */\n  walletAdapter?: never;\n  /** Manual supply does not accept a sender account. */\n  account?: never;\n  /** Manual supply does not accept an execution amount. */\n  amount?: never;\n}\n\ninterface WalletTransferSupplyFlowRequest extends BaseSupplyFlowRequest {\n  /** Transfer supply uses the default mechanism and does not accept this field. */\n  mechanism?: never;\n  /** Wallet adapter used to broadcast the transfer. */\n  walletAdapter: Pick<\n    WalletAdapter,\n    "sendBtcTransaction" | "sendEthTransaction" | "sendIcrcTransfer"\n  >;\n  /** Sender wallet account. */\n  account: string;\n  /** Transfer amount in the target asset\'s base units. */\n  amount: bigint;\n}\n\ninterface BaseSupplyFlowRequest {\n  profileId: string;\n  poolId: string;\n  action: SupplyAction;\n  /** Transfer chain to use. Pass ICP for ck-ledger transfers. */\n  chain: Chain;\n}\n\ntype SupplyAction = "deposit" | "repayment";\n\ntype Chain = "BTC" | "ETH" | "ICP";',
  "lending.estimateInflowFee":
    'client.lending.estimateInflowFee(request: AssetIdentifier): Promise<InflowFeeEstimate>\n\ntype EstimateInflowFeeRequest = AssetIdentifier;\n\ntype AssetIdentifier =\n  | { chain: "BTC"; asset: "BTC" }\n  | { chain: "ETH"; asset: "USDC" }\n  | { chain: "ETH"; asset: "USDT" }\n  | { chain: "ICP"; asset: "BTC" }\n  | { chain: "ICP"; asset: "ICP" }\n  | { chain: "ICP"; asset: "USDC" }\n  | { chain: "ICP"; asset: "USDT" };',
  "lending.submitInflow":
    'client.lending.submitInflow(request: SubmitInflowRequest): Promise<SubmitInflowResponse>\n\ninterface SubmitInflowRequest extends SubmitSupplyFlowInflowRequest {\n  /** Deposit or repayment operation represented by the transaction. */\n  operation: InflowOperation;\n}\n\ninterface SubmitSupplyFlowInflowRequest {\n  /** Broadcast transaction id or hash. */\n  txid: string;\n  /** Chain where the transaction was broadcast, when not implied by the flow. */\n  chain?: Extract<Chain, "BTC" | "ETH">;\n}\n\ntype InflowOperation = "deposit" | "repayment";\n\ntype Chain = "BTC" | "ETH" | "ICP";\n\ntype LiquidiumOperation = "deposit" | "borrow" | "repayment" | "withdrawal" | "liquidation";',
  "lending.isBorrowingDisabled":
    "client.lending.isBorrowingDisabled(): Promise<boolean>\n\nNo request object type.",
};
